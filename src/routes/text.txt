<script>


	let bestellingen = [];
	let snackInput = '';
	let naamInput = '';
	let frietInput = '';

	let snacks = [
		'Bamischijf Pittig',
		'Gehaktstaaf',
		'Kipkorn',
		'Viandelle',
		'Bal Gehakt (frituur)',
		'Goulash- of Satékroket',
		'Groentekroket',
		'Kaaskroket',
		'Kalfs- of Dobben Kroket',
		'Mexicano',
		'Braadworst',
		'Satérol',
		'Hamburger',
		'Berenklauw',
		'Sitostick',
		'Shoarmarol',
		'Bal Gehakt (jus)',
		'Frikandel XXL',
		'Loempia',
		'Mexicano XXL',
		'Kipcornwrap',
		'Mexicano wrap'
	];

	let friet = ['Friet', 'Friet met mayonaise', 'Friet speciaal', 'Friet oorlog'];

	// moet met objects namen toevoegen zodat je weet wie besteld
	// moet er voor zorgen dat als de bestelling hetzelfde is dat er een 2x komt te staan

	function addBestelling() {
		if (naamInput.trim() === '' || snackInput.trim() === '') return;

		let bestelling = {
			naam: naamInput,
			snack: snackInput
		};

		bestellingen.push(bestelling);

		naamInput = '';
		snackInput = '';

		bestellingen = bestellingen;
	}

	function verwijderBestelling(index) {
		bestellingen.splice(index, 1);

		bestellingen = bestellingen;
	}

	$: totaal = bestellingen.length;

	//function addSnack(){
	// if (snackInput.trim() !== "")
	//snackLijst.push(snackInput);
	//    snackInput "";
	//   snackLijst = snackLijst;
	// }
</scri




<div class="Title">
	<h1>Wat wil je bestellen?</h1>
</div>

<div class="snacktext">
	{#each bestellingen as bestelling, index}
		<p>
			{bestelling.naam} bestelt: {bestelling.snack}

			<button on:click={() => verwijderBestelling(index)}> verwijder </button>
		</p>
	{/each}

	<input bind:value={naamInput} placeholder="naam" />

	<input bind:value={snackInput} placeholder="voeg een snack toe" />

	<button on:click={addBestelling}>
		<p>toevoegen</p>
	</button>

	<p>je hebt {totaal} snacks</p>
</div>

<style>
	.Title {
		font-size: 30px;
		text-align: center;
	}

	.snacktext {
		font-size: 15px;
		text-align: center;
	}
</style>
